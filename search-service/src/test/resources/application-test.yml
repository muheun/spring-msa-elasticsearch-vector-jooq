server:
  port: 0

elasticsearch:
  uris: http://localhost:9200

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

logging:
  level:
    me.muheun.moaspace.search: DEBUG
    org.elasticsearch: WARN

cdc:
  topic-handlers:
    posts.posts.posts: post-document
